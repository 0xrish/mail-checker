{
    "title": "Email Checker Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "email": {
            "title": "Single Email",
            "type": "string",
            "description": "A single email address to check.",
            "editor": "textfield",
            "default": "xyz.mikolabs@gmail.com",
            "examples": ["xyz.mikolabs@gmail.com"]
        },
        "emails": {
            "title": "List of Emails",
            "type": "array",
            "description": "A list of email addresses to check.",
            "editor": "stringList",
            "items": {
                "type": "string"
            }
        },
        "from_email": {
            "title": "From Email",
            "type": "string",
            "description": "The email to use in the MAIL FROM: SMTP command.",
            "default": "reacher.email@gmail.com",
            "editor": "textfield"
        },
        "hello_name": {
            "title": "Hello Name",
            "type": "string",
            "description": "The name to use in the EHLO: SMTP command.",
            "default": "gmail.com",
            "editor": "textfield"
        },
        "check_gravatar": {
            "title": "Check Gravatar",
            "type": "boolean",
            "description": "Whether to check if a gravatar image exists for the email.",
            "default": false,
            "editor": "checkbox"
        },
        "haveibeenpwned_api_key": {
            "title": "HaveIBeenPwned API Key",
            "type": "string",
            "description": "Optional API key for HaveIBeenPwned to check for breaches.",
            "editor": "textfield"
        },
        "proxy_url": {
            "title": "Proxy URL",
            "type": "string",
            "description": "Optional SOCKS5 proxy URL (e.g., socks5://user:pass@host:port).",
            "editor": "textfield"
        },
        "smtp_timeout": {
            "title": "SMTP Timeout (seconds)",
            "type": "integer",
            "description": "Timeout for each SMTP connection in seconds.",
            "default": 15,
            "minimum": 1,
            "editor": "number"
        }
    }
}